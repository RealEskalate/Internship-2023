import 'package:flutter/material.dart';

// Widget colors
const scaffoldBackGroundColor = Color.fromRGBO(248, 250, 255, 1);
const menuIconColor = Colors.black;
const profileAvatarCircularRingColor = Colors.white;
const filterChipsColor = Color.fromRGBO(55, 106, 237, 1);
const cardTagColor = Color.fromRGBO(94, 95, 111, 1);
const welcomeTextColor = Color(0xff0D253C);
const provideCredenTextColor = Color(0xff2D4379);
const primaryColor = Color(0xFF376AED);
const secondaryColor = Color(0xFFFFBA25);

// Text colors
const defaultTextColor = Colors.black;
const secondaryTextColor = Color(0xFF211772);

// Shadow colors
const menuIconShadowColor = Color.fromRGBO(0, 0, 0, 0.25);

// Icon colors
const defaultIconColor = Colors.white;

// Button colors
const defaultButtonColor = Color.fromRGBO(102, 154, 255, 1);

const borderShadow = Color.fromRGBO(79, 91, 121, 0.1);
