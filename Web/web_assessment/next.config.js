/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['media.licdn.com','picsum.photos','res.cloudinary.com']
  },
}

module.exports = nextConfig
